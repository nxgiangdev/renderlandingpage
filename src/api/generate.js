// This would be a Next.js API route, but we're using Vite
// Instead, we'll create a service that calls a backend API
// For now, let's keep it client-side but ensure API key is passed correctly

export const generateViaAPI = async (prompt, apiKey) => {
  // This is a placeholder - in production, you'd call your backend API
  // For now, we'll handle it in the service directly
  return null
}

